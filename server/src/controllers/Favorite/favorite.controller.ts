import { Request, Response } from "express";
import { db } from "../../config/db";
import { favoritesTable } from "../../config/schema";

export const favoriteController = async (req : Request, res:Response) => {
    
    
}